<div class="admin-box">
  <div class="demande-compo-form">
    <h2>Demande de Composteur</h2>
    <%= simple_form_for(@demande) do |f| %>
      <div class="formline-between mt-2">
        <%= f.input :first_name, placeholder: 'Prénom' %>
        <%= f.input :last_name, placeholder: 'Nom' %>
      </div>
      <div class="formline-between">
        <%= f.input :email, placeholder: 'Email' %>
        <%= f.input :phone_number, placeholder: 'Téléphone' %>
      </div>
      <%= f.input :address, placeholder: 'Adresse' %>
      <%= f.input :photo, label: "Avez-vous une photo du lieu envisagé ?", as: :file %>
      <%= f.input :logement_type,
                  label: 'Dans quel type de logement vivez-vous ?',
                  as: :radio_buttons,
                  collection: [["maison", "Je vis dans une maison."], ["appartement", "Je vis dans un appartement."]],
                  label_method: :last,
                  value_method: :first%>
      <%= f.input :inhabitant_type,
                  label: 'Etes vous proprietaire, locataire ou syndic ?',
                  as: :radio_buttons,
                  collection: ['proprietaire', 'locataire', 'syndic'] %>
      <%= f.input :potential_users,
                  label: "Connaissez-vous d'autres personnes interessées par un composteur dans ce quartier ?",
                  as: :radio_buttons,
                  collection: [[true, "oui ! 🥳"], [false, "non..😕"]],
                  label_method: :last,
                  value_method: :first %>
      <%= invisible_captcha %>
      <div class="submit-formline-center">
        <%= f.submit "Envoyer ma demande !", class: "bouton-valid" %>
      </div>
    <% end %>
    <div class="demande-form-line"></div>
  </div>
</div>
